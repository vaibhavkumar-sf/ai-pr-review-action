The Mermaid diagrams have syntax errors. Fix them and return valid JSON.

{{error_sections}}Common fixes: Remove emojis from labels. Quote ALL labels. Use -->|"label"| not commas. No par in flowcharts. No colons in labels.

